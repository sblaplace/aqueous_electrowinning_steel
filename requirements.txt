# Core scientific computing
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Data handling
pandas>=2.0

# Runtime deps that must mirror pyproject.toml [project.dependencies].
# numba is load-bearing: without it models/transport.py falls back to the
# scipy LSODA integration path, which is ~17x slower per Nernst-Planck solve
# (the 2026-08 discovery behind slow sandbox/CI test runs).
numba>=0.60
pyyaml>=6.0

# Electrochemistry-specific
# pourbaix (custom, see models/pourbaix.py)

# Jupyter (optional, for notebooks)
jupyter>=1.0
ipykernel>=6.0

# Visualization
seaborn>=0.12

# Units handling (optional but recommended)
pint>=0.22

# Testing
pytest>=7.0
pytest-xdist>=3.0          # required by Makefile's parallel test targets
pytest-benchmark>=4.0
scikit-optimize
